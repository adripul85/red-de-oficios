---

---

<div
    class="bg-white p-6 rounded-2xl shadow-md border-l-4 border-blue-600 flex flex-col md:flex-row justify-between items-center gap-4">
    <div class="flex items-center gap-4">
        <div class="bg-blue-100 p-3 rounded-full text-2xl"></div>
        <div>
            <h3 class="font-bold text-lg text-gray-900">
                Blindaje de Identidad
            </h3>
            <p class="text-sm text-gray-500">
                Sube DNI y Antecedentes para activar tu perfil.
            </p>
        </div>
    </div>
    <div class="flex gap-3">
        <div class="text-center">
            <input
                type="file"
                id="file-dni"
                class="hidden"
                accept="image/*,application/pdf"
            />
            <label
                for="file-dni"
                class="cursor-pointer text-xs font-bold bg-gray-100 hover:bg-gray-200 px-3 py-2 rounded block text-gray-700 transition"> DNI</label>
            <p id="status-dni" class="text-[10px] mt-1 text-red-500 font-bold">
                Pendiente
            </p>
        </div>
        <div class="text-center">
            <input
                type="file"
                id="file-ant"
                class="hidden"
                accept="image/*,application/pdf"
            />
            <label
                for="file-ant"
                class="cursor-pointer text-xs font-bold bg-gray-100 hover:bg-gray-200 px-3 py-2 rounded block text-gray-700 transition"> Antecedentes</label>
            <p id="status-ant" class="text-[10px] mt-1 text-red-500 font-bold">
                Pendiente
            </p>
        </div>
    </div>
</div>

