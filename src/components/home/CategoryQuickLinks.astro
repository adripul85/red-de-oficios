---
import { CATEGORIES } from "../../data/categories";

// 1. Mapa de Iconos (Coincidiendo con tus nombres exactos)
const iconMap: Record<string, string> = {
  // Urgencias
  "Plomero (Urgencias)": "🚨",
  "Electricista (Urgencias)": "⚡",
  "Gasista (Urgencias)": "🔥",
  "Cerrajero (24hs)": "🔑",
  // Construcción
  Albañil: "🧱",
  Pintor: "🎨",
  Techista: "🏠",
  "Colocador de Pisos / Revestimientos": "🔲",
  Durlockero: "🏗️",
  Herrero: "⚒️",
  Carpintero: "🪚",
  // Mantenimiento
  Jardinero: "🌿",
  Piletero: "🏊",
  Persianista: "🪟",
  Vidriero: "🪟",
  Fumigador: "🦟",
  Limpieza: "🧹",
  // Servicios
  "Fletero / Mudanzas": "🚚",
  "Técnico de Aire Acondicionado": "❄️",
  "Técnico de Electrodomésticos": "🔌",
  "Instalador de Cámaras y Alarmas": "📹",
  // Estética
  "Peluquero / Estilista": "💇‍♀️",
  "Manicura / Pedicura": "💅",
  "Maquillador / Maquilladora": "💄",
  Masajista: "💆‍♀️",
  // Eventos
  "Pastelero / Catering": "🎂",
  "DJ / Sonidista": "🎧",
  Tapicero: "🛋️",
  "Zapatero / Modista": "🧵",
  default: "🛠️",
};

// 2. Aplanar lista completa para el JS
const allRubros = CATEGORIES.flatMap((grupo) =>
  grupo.trades.map((trade) => ({
    nombre: trade.name,
    // Usamos el nombre como slug para la búsqueda
    slug: trade.name,
    icon: iconMap[trade.name] || iconMap["default"],
  })),
);
---

<section class="quick-links-section">
  <div class="container">
    <h2 class="section-title">¿Qué estás buscando hoy?</h2>

    <div class="categories-grid" id="random-container">
      <span style="color:#9ca3af; font-size:0.9rem;"
        >Cargando sugerencias...</span
      >
    </div>

    <div
      id="rubros-data"
      data-list={JSON.stringify(allRubros)}
      style="display: none;"
    >
    </div>
  </div>
</section>

<script>
  // Script para manejar animaciones y lógica simple
  document.addEventListener("astro:page-load", () => {
    const container = document.getElementById("random-container");
    if (!container) return;

    // Categorias definidas por el usuario
    const mainCategories = [
      {
        name: "Urgencias",
        icon: "🚨",
        slug: "Urgencias",
        desc: "Plomero, Luz, Gas, Llaves",
        link: "/resultados?q=Urgencias",
      },
      {
        name: "Obra y Refacción",
        icon: "🏗️",
        slug: "Obra",
        desc: "Albañil, Pintor, Techista",
        link: "/resultados?q=Construcción",
      },
      {
        name: "Hogar",
        icon: "🏠",
        slug: "Hogar",
        desc: "Jardín, Pileta, Persiana",
        link: "/resultados?q=Mantenimiento",
      },
      {
        name: "Servicios",
        icon: "🚛",
        slug: "Servicios",
        desc: "Fletes, Aire, Reparaciones",
        link: "/resultados?q=Servicios",
      },
      {
        name: "Estética",
        icon: "💇",
        slug: "Estética",
        desc: "Peluquería, Manicura, Masaje",
        link: "/resultados?q=Estética",
      },
      {
        name: "Eventos",
        icon: "🥳",
        slug: "Eventos",
        desc: "Pastelería, DJ, Sonido",
        link: "/resultados?q=Eventos",
      },
    ];

    // GENERAR HTML
    const cardsHTML = mainCategories
      .map(
        (cat) => `
      <a href="${cat.link}" class="category-card fade-in">
        <span class="icon">${cat.icon}</span>
        <span class="name">${cat.name}</span>
        <span class="desc" style="font-size: 0.7rem; color: #9ca3af; text-align: center;">${cat.desc}</span>
      </a>
    `,
      )
      .join("");

    container.innerHTML = cardsHTML;
  });

  // Fallback por si no usas View Transitions
  document.addEventListener("DOMContentLoaded", () => {
    document.dispatchEvent(new Event("astro:page-load"));
  });
</script>

<style>
  .quick-links-section {
    padding: 40px 0;
    background: #ffffff;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .section-title {
    font-size: 1.5rem;
    color: #1f2937;
    margin-bottom: 25px;
    font-weight: 700;
  }

  .categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
    gap: 15px;
    min-height: 200px;
  }

  /* ESTILOS GLOBALES PARA ELEMENTOS INYECTADOS */
  :global(.category-card) {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: #f9fafb;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    padding: 20px 10px;
    text-decoration: none;
    transition: all 0.2s ease;
    cursor: pointer;
    height: 100%;
    min-height: 110px;
    animation: fadeIn 0.5s ease-in-out;
  }

  :global(.category-card:hover) {
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.05);
    border-color: #e67e22;
    background: white;
  }

  :global(.category-card .icon) {
    font-size: 2rem;
    margin-bottom: 10px;
  }

  :global(.category-card .name) {
    color: #4b5563;
    font-weight: 600;
    font-size: 0.85rem; /* Ajustado para nombres largos */
    text-align: center;
    line-height: 1.2;
  }

  :global(.view-all) {
    background: #fff7ed !important;
    border-color: #ffedd5 !important;
  }
  :global(.view-all .name) {
    color: #ea580c !important;
  }
  :global(.view-all:hover) {
    background: #ffedd5 !important;
    border-color: #ea580c !important;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(5px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 600px) {
    .categories-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 10px;
    }
    :global(.category-card) {
      padding: 15px 5px;
      min-height: 90px;
    }
    :global(.category-card .icon) {
      font-size: 1.5rem;
      margin-bottom: 5px;
    }
    :global(.category-card .name) {
      font-size: 0.7rem;
    }
  }
</style>
